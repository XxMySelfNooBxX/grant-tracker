---
description: Start the development servers
---

This workflow starts both the React frontend and the Node.js backend for local development.

// turbo
1. Start the backend by running the command `node index.js` in the `server` directory.

// turbo
2. Start the frontend by running the command `npm start` in the `client` directory.
